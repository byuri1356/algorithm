#include <iostream>
#include <vector>
#include<cstring>
#include<string>
#include<map>
#include<algorithm>
using namespace std;



int main()
{
    int N, M;
    cin >> N >> M;

    for (int i = 0; i < N; ++i) {
        int a, b;
        cin >> a >> b;
    }
    cout << "비와이";
   
    return 0;
}
