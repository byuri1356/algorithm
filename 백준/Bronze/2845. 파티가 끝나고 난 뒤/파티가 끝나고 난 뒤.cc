#include<iostream>
#include<vector>
#include<algorithm>
#define fastio cin.tie(0)->ios::sync_with_stdio(0); cout.tie(0); setvbuf(stdout, nullptr, _IOFBF, BUFSIZ);
using namespace std;
using ll = long long;



int main() {

	fastio;

	int a, b, c, d, e, f, g; cin >> a >> b >> c >> d >> e >> f >> g;
	cout << c - a * b << " " << d - a * b << " " << e - a * b << " " << f - a * b << " " << g - a * b;





}